@model IEnumerable<GestaoDoConecimento.Dados.Solucao>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Atividade.NomeAtividade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NomeSolucao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Atividade.NomeAtividade)
        </td>
        <td>
            @Html.ActionLink(item.NomeSolucao.ToString(), "Details", new { id = item.Id })
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
    </tr>
}

</table>
